/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        // loader: 'akamai', // Możemy zmienić na wybranego dostawcę
        // path: '',
        domains: ['images.unsplash.com'],
    },
};

export default nextConfig;



